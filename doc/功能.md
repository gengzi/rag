## 功能点
* 将aippt 构建成一个多agent 任务，采用react agent 模型来构建。
-支持人类反馈
- 调用rag 获取知识库信息（大纲生成，每页内容生成）
- 多模态模型，生成一些插图（也抽象为工具调用）

- 短期记忆的一些扩展，剪枝，压缩等等

* 长期记忆的简化实现 （将每次用户对话，通过llm 提取出 用户个性的信息，将其存入数据库中，再进行embedding ，存入向量数据库。再用户提问时，通过问题和 向量数据库的用户记忆相似度计算，提取出topk，加入到聊天对话记录中）